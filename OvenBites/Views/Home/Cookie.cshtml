@model DisplayViewModel
<section class="section-container">
    <div class="section-content">
        <h2>@Model.CurrentPage.PageTitle</h2>
        <p>@Html.Raw(Model.CurrentPage.Description)</p>
    </div>
</section>